# send-to-s3

```bash
$ go get github.com/hawx/send-to-s3
$ echo $AWS_ACCESS_KEY_ID
...
$ echo $AWS_SECRET_ACCESS_KEY
...
$ send-to-s3 --bucket coolbucket --name amazon.jpg --src local.jpg
$ send-to-s3 --help
```
